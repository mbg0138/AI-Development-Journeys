# Render / production secret (optional for Open-Meteo free tier)
API_KEY=

# Default coordinates when query params are omitted
WEATHER_LATITUDE=41.0082
WEATHER_LONGITUDE=28.9784

# Optional overrides
# WEATHER_API_URL=https://api.open-meteo.com/v1/forecast
# GEOCODING_API_URL=https://nominatim.openstreetmap.org/reverse
# REQUEST_TIMEOUT=10.0
NOMINATIM_USER_AGENT=your-app-name/1.0 (you@example.com)

# Local Flask dev server port
PORT=5000
